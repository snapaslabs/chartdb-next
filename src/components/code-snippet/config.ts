// In Next.js, @monaco-editor/react loads Monaco from CDN by default.
// Worker bundling via ?worker syntax is Vite-specific; omitting it here
// lets @monaco-editor/react use its built-in CDN worker setup.
export {};
